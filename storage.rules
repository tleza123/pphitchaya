rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Private media is served by the authenticated server API.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
