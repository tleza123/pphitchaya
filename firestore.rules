rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // New project only. Business access is through owner-checked Admin APIs.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
